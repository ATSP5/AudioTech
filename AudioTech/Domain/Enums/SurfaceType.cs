namespace AudioTech.Domain.Enums;

public enum SurfaceType
{
    Hard,   // concrete, brick — low absorption (~0.05)
    Mixed,  // wood, plaster  — medium absorption (~0.30)
    Soft    // carpet, curtain — high absorption (~0.80)
}
